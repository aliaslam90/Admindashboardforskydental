{
  "buildCommand": "cd frontend && (npm install || (rm -rf node_modules package-lock.json && npm install)) && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd frontend && npm install || (rm -rf node_modules package-lock.json && npm install)",
  "framework": "vite"
}

