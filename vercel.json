{
  "buildCommand": "cd frontend && rm -rf node_modules package-lock.json && npm install --include=dev && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd frontend && rm -rf node_modules package-lock.json && npm install --include=dev",
  "framework": "vite"
}

